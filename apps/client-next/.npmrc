# Force npm to ignore peer dependency conflicts during CI installs.
# Cloudflare Pages runs `npm ci` by default and doesn't always expose an
# "Install command" field in some UI configurations; setting this here
# ensures `--legacy-peer-deps` behavior during the Pages build.
legacy-peer-deps=true

# Optional: disable funding prompts in CI logs
fund=false
